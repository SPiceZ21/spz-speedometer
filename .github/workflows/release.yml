name: Release

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

# One release run at a time per repo. Each run now deletes the previous
# release before publishing its own, so two pushes close together would
# otherwise race: the second run could delete the release the first one had
# only just created, and leave the repo with nothing published at all.
concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install & Build UI
        shell: bash
        run: |
          if [ -d "ui" ] && [ -f "ui/package.json" ]; then
            echo "Building UI in ui/ folder..."
            cd ui
            npm install --no-audit --no-fund
            [ -d "node_modules/.bin" ] && chmod +x node_modules/.bin/* || true
            npm run build
            cd ..
          elif [ -d "html" ] && [ -f "html/package.json" ]; then
            echo "Building UI in html/ folder..."
            cd html
            npm install --no-audit --no-fund
            [ -d "node_modules/.bin" ] && chmod +x node_modules/.bin/* || true
            npm run build
            cd ..
          fi

      - name: Package Resource
        shell: bash
        run: |
          RESOURCE_NAME=$(basename $(pwd))
          PKG_DIR="release-pkg/$RESOURCE_NAME"
          mkdir -p "$PKG_DIR"
          
          # Copy Lua scripts and assets
          [ -d "client" ] && cp -r client "$PKG_DIR/"
          [ -d "server" ] && cp -r server "$PKG_DIR/"
          [ -d "shared" ] && cp -r shared "$PKG_DIR/"
          [ -d "data" ] && cp -r data "$PKG_DIR/"
          [ -d "locales" ] && cp -r locales "$PKG_DIR/"
          [ -f "fxmanifest.lua" ] && cp fxmanifest.lua "$PKG_DIR/"
          [ -f "config.lua" ] && cp config.lua "$PKG_DIR/"
          
          # Smart UI Packaging
          if [ -d "ui/dist" ]; then
            echo "Packaging Vite UI (ui/dist)"
            mkdir -p "$PKG_DIR/ui/dist"
            cp -r ui/dist/* "$PKG_DIR/ui/dist/"
          elif [ -d "html/dist" ]; then
            echo "Packaging Vite UI (html/dist)"
            mkdir -p "$PKG_DIR/html/dist"
            cp -r html/dist/* "$PKG_DIR/html/dist/"
          elif [ -d "ui" ]; then
            echo "Packaging Raw UI (ui/)"
            mkdir -p "$PKG_DIR/ui"
            cp -r ui/* "$PKG_DIR/ui/"
            rm -rf "$PKG_DIR/ui/node_modules" "$PKG_DIR/ui/src" 2>/dev/null || true
          elif [ -d "html" ]; then
            echo "Packaging Raw UI (html/)"
            mkdir -p "$PKG_DIR/html"
            cp -r html/* "$PKG_DIR/html/"
            rm -rf "$PKG_DIR/html/node_modules" "$PKG_DIR/html/src" 2>/dev/null || true
          fi
          
          cd release-pkg
          zip -rq "../${RESOURCE_NAME}.zip" "$RESOURCE_NAME"

      - name: Generate Release Notes
        id: notes
        shell: bash
        run: |
          TAG="v$(date +"%Y.%m.%d")-${{ github.run_number }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "## Release Notes" > release_notes.md
          echo "Built from commit \`${{ github.sha }}\`" >> release_notes.md
          
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            git log --oneline --no-merges -n 20 >> release_notes.md
          else
            git log ${LAST_TAG}..HEAD --oneline --no-merges >> release_notes.md
          fi

      # Runs AFTER the notes step on purpose. The notes are "commits since the
      # previous release", worked out from that release's tag — which the
      # checkout above fetched. Deleting it any earlier would leave nothing to
      # diff against, and every release would fall back to the last 20 commits.
      - name: Remove previous releases
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # --cleanup-tag removes the git tag along with the release, so the
          # tag list stays as short as the release list instead of piling up
          # one date-stamped tag per push forever.
          gh release list --repo "${{ github.repository }}" --limit 100 \
            --json tagName --jq '.[].tagName' |
          while read -r tag; do
            [ -z "$tag" ] && continue
            echo "Removing previous release $tag"
            gh release delete "$tag" --repo "${{ github.repository }}" --yes --cleanup-tag
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.notes.outputs.tag }}
          name: "Release ${{ steps.notes.outputs.tag }}"
          body_path: release_notes.md
          files: "*.zip"
          make_latest: true
